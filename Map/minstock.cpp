//sauce: https://www.spoj.com/problems/MINSTOCK/

#include<stdio.h>
#include<stdlib.h>
#include<map>
#include <unordered_map>
#include<string>

using namespace std;

int main()
{
    int n,a,b;
    char s[20];
    scanf("%d",&n);

    unordered_map<string, int> mp;

    for(int i=0;i<n;i++)
    {
        scanf("%d",&q);

        if(q<3)
        {
            scanf("%s %d"s,&a);

            mp[s]=a;
        }

        else if(q==3)
        {
            int c = mp.size();

            for(int i=0;i<c;i++)
            {

            }
        }


    }
}